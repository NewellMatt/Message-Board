{{#link-to 'question' question.id}}
  <div>
    <h1>{{question.content}}</h1>
    {{fullQuestionInfo}}
  </div>
{{/link-to}}

<button class="btn btn-primary btn-sm" {{action "addToCart" question}}>Mark As Favorite Question</button>
